include ':survey_app'

def libraryProjectGradle = new File('../androidlibrary/androidlibrary_lib/build.gradle')
if (libraryProjectGradle.isFile()) { // check if local project is present
    include ':androidlibrary:androidlibrary_lib'
    project(':androidlibrary:androidlibrary_lib').setProjectDir(new File('../androidlibrary/androidlibrary_lib')) // set local location
}

def commonProjectGradle = new File('../androidcommon/androidcommon_lib/build.gradle')
if (commonProjectGradle.isFile()) { // check if local project is present
    include ':androidcommon:androidcommon_lib'
    project(':androidcommon:androidcommon_lib').setProjectDir(new File('../androidcommon/androidcommon_lib')) // set local location
}
